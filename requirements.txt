# AI Music Contract Analyzer — Python dependencies (Windows desktop app)
# Install:  python -m venv .venv ; .venv\Scripts\activate ; pip install -r requirements.txt

# --- GUI ---
PySide6>=6.6,<6.9

# --- Core / analysis ---
anthropic>=0.40            # Claude SDK (model: claude-opus-4-8)
pydantic>=2.6,<3           # domain models (single source of truth)

# --- Document parsing ---
PyMuPDF>=1.24             # fast PDF text + layout extraction (import: fitz)
pdfplumber>=0.11          # fallback PDF extraction with word boxes
python-docx>=1.1          # DOCX parsing + report generation

# --- Secure key storage ---
keyring>=24               # Windows Credential Manager for the API key

# --- Optional: analysis history (SQLite via SQLAlchemy) ---
SQLAlchemy>=2.0

# --- Optional: PDF report output ---
# docx2pdf requires Microsoft Word installed. Left optional; DOCX is the default.
# docx2pdf>=0.1.8

# --- Dev / test ---
pytest>=8.0
